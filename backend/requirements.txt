# HMS Backend — Python Dependencies

# Django Core
Django==5.1.7

# Django REST Framework
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1

# Environment variable & Database management
python-dotenv==1.2.2
dj-database-url==2.1.0
psycopg2-binary

# CORS headers
django-cors-headers==4.4.0

# Email — SendGrid
sendgrid==6.11.0

# Storage — Cloudinary (credentials added later via .env)
cloudinary==1.40.0

# Storage — Dropbox (credentials added later via .env)
dropbox==12.0.2

# HTTP requests (used by storage services)
requests==2.32.3

# Production server
gunicorn==23.0.0

# Static file serving in production
whitenoise==6.9.0
